apiVersion: v1
kind: Service
metadata:
  name: spark-service
  namespace: default  # Change if you are using a different namespace
spec:
  selector:
    app: spark
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8080
  type: LoadBalancer  # Adjust if you need an external IP or LoadBalancer